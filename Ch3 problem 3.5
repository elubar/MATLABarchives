%part a) inputs: 1,1,1 part a) outputs: -.5 and +-.866i
%part b) inputs: 2 4 2 part b) outputs: -1 and -1
%part c) inputs: 2, 2, -12 part c) outputs: 2 and -3

function x = quadratic(a,b,c)
%input('what is your a value?')
if a==0 & b==0 & c==0
    disp('  ')
    disp('Solution indeterminate')
elseif a == 0 & b==0
    disp('  ')
    disp('There is no solution')
elseif a == 0
    disp('  ')
    disp('Only one root: this equation is linear')
    disp('x')
    x1 = -b/c
    x2 = NaN;
elseif b^2 < 4*a*c
    disp('  ')
    disp(' x1, x2 are complex roots')
    disp('x1, x2')
    x1 = ((-b+sqrt(b^2 - 4*a*c))/(2*a));
    x2 = ((-b-sqrt(b^2 - 4*a*c))/(2*a));
elseif b^2 == 4*a*c
    x1 = -b/(2*a)
    x2 = x1
    disp('equal roots')
    disp('x1 x2')
else
    x1 = ((-b+sqrt(b^2 - 4*a*c))/(2*a));
    x2 = ((-b-sqrt(b^2 - 4*a*c))/(2*a));
    %disp('x1 x2')
end
if a == 0 & b == 0 & c == 0
elseif  a == 0 & b == 0
else
    disp([x1 x2]);
end
